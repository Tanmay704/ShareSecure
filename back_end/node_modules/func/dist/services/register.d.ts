import { OptionParams, CommandParams } from '../interfaces';
export declare class RegisterProvider {
    private _commands;
    private _options;
    static isRegisterProvider: boolean;
    constructor(_commands?: CommandParams[], _options?: OptionParams[]);
    get commands(): CommandParams[];
    get options(): OptionParams[];
}
